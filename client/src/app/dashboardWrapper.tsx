import React from 'react'

type Props = {}

const DashboardWrapper = ({children } :{children: React.ReactNode } ) => {
  return (
    <div className='flex min-h-screen w-full bg-gray text-gray-900'>dashboardWrapper</div>
  )
}

export default DashboardWrapper